// import { createAction, props } from "@ngrx/store";

// export const loadProduct = createAction('[Products] Load Products');
// export const loadedProducts = createAction('[Products] Loaded Products', props<{products: any}>);
